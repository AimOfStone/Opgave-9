# Opgave-9
Ball simulator

The program draws coloured balls with random variables (speed, position, velocity, "leader)
each ball follows another random ball. If they an edge they wrap around.

1. loop through all balls
2. create a random ball for each iteration of the loop
3. Update the elements/variables
x. repeat #3 until window is closed
